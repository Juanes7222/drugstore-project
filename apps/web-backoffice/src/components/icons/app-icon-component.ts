import type { ComponentType } from "react";
import type { AppIconProps } from "./app-icons";

/** Any generated AppIcon component (drop-in compatible with MUI sizing). */
export type AppIconComponent = ComponentType<AppIconProps>;
