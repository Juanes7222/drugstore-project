DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pharmacy-system
REDIS_URL=redis://localhost:6379

# Max pooled DB connections for this worker process (BullMQ job concurrency
# determines how many are held at once).
DB_POOL_MAX=10

# Certificate base directory for FileSystemSecretReaderAdapter.
# "file:"-prefixed credential references are resolved relative to this path.
CERTIFICATE_BASE_DIR=./certs

# Private-key password for all .p12 certificates resolved through
# FileSystemSecretReaderAdapter. In production this must be stored
# in a proper secret manager, not in an environment variable.
DIAN_CERTIFICATE_PASSWORD=change-me

# ----------------------------------------------------------------------------
# Infisical (secrets manager) — used ONLY in production
# ----------------------------------------------------------------------------
# In production the app resolves secrets exclusively from Infisical using a
# Machine Identity (Universal Auth). These credentials are injected by the
# deployment platform (Kubernetes/Docker secrets), never from this file, and
# the app fails to boot if they are missing.
# Infisical values win over any other environment variable (except INFISICAL_*),
# so store the full key set documented here in the 'prod' environment.
# INFISICAL_CLIENT_ID=
# INFISICAL_CLIENT_SECRET=
# INFISICAL_PROJECT_ID=
# INFISICAL_ENVIRONMENT=prod
# INFISICAL_SITE_URL=https://app.infisical.com
# INFISICAL_SECRET_PATH=/
#
# Development keeps using this .env file and never contacts Infisical. Set
# INFISICAL_ENABLED=true (plus the credentials above) to exercise the
# integration locally against the 'dev' Infisical environment.
# INFISICAL_ENABLED=false

# Base64-encoded 32-byte key for decrypting fiscal certificates at rest (must match server)
FISCAL_CERTIFICATE_KEK_BASE64=
