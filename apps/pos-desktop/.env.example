# API base URL for the POS renderer.
# Set this to the running NestJS server (e.g. http://localhost:3000).
# If unset, the Sales/Cart screen falls back to the mock catalog service.
VITE_API_BASE_URL=
