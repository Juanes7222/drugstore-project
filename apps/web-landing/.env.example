# Base URL of the NestJS server. The checkout form posts to
# {VITE_API_URL}/public/licensing/checkout/create-session.
# Leave empty to run the site without a working purchase flow (the
# checkout dialog shows a configuration error instead of failing silently).
VITE_API_URL=http://localhost:3000
