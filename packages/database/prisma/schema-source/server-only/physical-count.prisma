// Server-only schema fragment — only included in the full build.
// Physical inventory counts, tenant-scoped. sequentialNumber is unique per
// subscription.
// This file is generated by scripts/split-schema.mjs.

model PhysicalCount {
  id                String    @id
  subscriptionId    String
  sequentialNumber  Int
  state             PhysicalCountState @default(OPEN)
  startedAt         DateTime
  startedByUserId   String
  startedByUser     User      @relation("StartedByUser", fields: [startedByUserId], references: [id], onDelete: NoAction)
  finishedAt        DateTime?
  approvedAt        DateTime?
  approvedByUserId  String?
  approvedByUser    User?     @relation("ApprovedByUser", fields: [approvedByUserId], references: [id], onDelete: NoAction)
  appliedAt         DateTime?
  notes             String?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
  adjustmentDocuments InventoryAdjustmentDocument[]

  @@index([subscriptionId])
  @@unique([subscriptionId, sequentialNumber])
  @@index([state])
  @@index([startedAt])
}